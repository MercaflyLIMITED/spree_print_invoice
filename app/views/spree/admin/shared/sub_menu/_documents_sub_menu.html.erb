<ul id="sidebar-documents" class="collapse nav nav-pills nav-stacked">
  <%= tab(:invoice_docs, url: spree.admin_bookkeeping_documents_path(q: { template_eq: 'invoice' })) if can?(:manage, Spree::BookkeepingDocument) %>
  <%= tab(:packaging_slips, url: spree.admin_bookkeeping_documents_path(q: { template_eq: 'packaging_slip' })) if can?(:manage, Spree::BookkeepingDocument) %>
</ul>
